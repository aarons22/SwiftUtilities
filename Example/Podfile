source 'https://github.com/CocoaPods/Specs.git'
use_frameworks!

target 'SwiftUtilities_Example', :exclusive => true do
  pod 'SwiftUtilities', :path => '../'
end

target 'SwiftUtilities_Tests', :exclusive => true do
  pod 'SwiftUtilities', :path => '../'

  pod 'Quick', '~> 0.8.0'
  pod 'Nimble', '3.0.0'
  pod 'FBSnapshotTestCase'
  pod 'Nimble-Snapshots'
end
